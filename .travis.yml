language: python
python:
    - "2.7"
sudo: false
cache:
    directories:
        - "~/.platformio"
install:
    - pip install -U platformio
script:
    - cd code ; platformio run -e itead-sonoff-basic
branches:
    only:
        - master
        - dev
